# URL pública do backend (Express). Em desenvolvimento local, pode omitir
# que o app usa http://localhost:3001 como padrão.
# Em produção (Vercel), defina com a URL do deploy do backend no Render/Railway.
NEXT_PUBLIC_API_URL=http://localhost:3001
